import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image: {
        height: 150,
        aspectRatio: 4/6,
        resizeMode: 'cover',
        borderRadius: 12,
        marginRight: 7,
        marginLeft: 5,
        // borderWidth: 1,
        // borderColor: 'white',
    }
});

export default styles;